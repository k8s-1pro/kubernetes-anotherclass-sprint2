spring:
  profiles:
    active: local

server:
  port: 8080

application:
  role: "ALL"
  version: "Api Tester v1.0.0"

filepath:
  postgresql:
  persistent-volume-data:
  temp-volume-data:

datasource:
  driver-class-name:
  url:
  username:
  password: